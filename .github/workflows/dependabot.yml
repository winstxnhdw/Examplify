name: Dependabot

on: pull_request

jobs:
  auto-merge:
    needs: test
    uses: winstxnhdw/actions/.github/workflows/dependabot-public.yml@main
    secrets:
      token: ${{ secrets.GITHUB_TOKEN }}
    permissions:
      contents: write
